using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlippedNode : Node
{
   /*private void updateTex()
    {
        mainTexure.clear();
        mainTexure.addTexture(baseTex);

        foreach (Node n in positiveNodes)
        {
            mainTexure.addTexture(n.GetGameTexture());
        }

        foreach (Node n in negativeNodes)
        {
            mainTexure.subTexture(n.GetGameTexture());
        }

    }*/
}
